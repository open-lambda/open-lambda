# Inf:
# A very simple handler that doesn't really do anything
# Used to test self destruction mechanism
def f(event):
    while(True):
        tombstone = 0
    return "Returned but shouldn't, something went wrong?"
